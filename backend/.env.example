# Server port
PORT=5000

# Optional: Add your Anthropic API key to enable AI-powered feedback.
# Without this, the analyzer still works fully using rule-based scoring.
ANTHROPIC_API_KEY=

# Max upload size in MB
MAX_UPLOAD_MB=5
